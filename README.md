Sources for the plugin `plugin.appodeal.beta.MillenialMedia`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.MillenialMedia" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
